4.2图 4-2中基本的单周期MIPS实现仅能实现某些指令。可以在这个指令集中加入新的指令，但决
定是否加人取决于给处理器的数据通路和控制通路增加的成本和复杂度。对下面的新指令而言，
试回答下列3个问题:
指令: LWI Rt,Rd (Rs)
解释: Reg[ Rt] = Mem[Reg[Rd] + Reg[Rs]]
4.2.1 [10]<4.1>对 上述指令而言，哪些已有的单元还可以被使用?
4.2.2 [10]<4.1>对 上述指令而言，还需要增加哪些功能单元?
4.2.3 [10]<4.1> 为了支持这些指令，需要在控制单元增加哪些信号?

答:
4.2.1
用到的单元有指令存储器,两个寄存器读取口，ALU(用来相加两个寄存器的值)，数据存储器，还有寄存器的写入口

4.2.2
不需要，现有的结构能实现该指令

4.2.3
不需要增加信号，就能实现该指令，只需要更改控制逻辑就行